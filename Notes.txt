Step 1:open Cmd type git
Step 2: git --version
git config --global user.name "Dependra pratap Singh"
git config --global user.email "dps@gmail.com"
mkdir
d: change for directory
create index files
type command on terminal git init
type Get-ChildItem
 type git add index.js
 type git diff
 type git add .
  type git commit -m"Intial code Added";
  type git log
  type git show id//two show changes in code
  type git log --oneline"for show id of commits"
  type git reset --hard<SHA>
  tyep git revert<SHA>
// github
Step 1: go to github profile
Step 2:Create New Repoesotriy
Step #:copy Repo link 
Step 4:come to the local Machine files